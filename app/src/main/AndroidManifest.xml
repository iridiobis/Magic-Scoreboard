<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="es.benitos.magicscoreboard" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/contadores"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.Contadores"
            android:label="@string/title_activity_contadores" >
        </activity>
        <activity
            android:name=".activities.NamePlayers"
            android:label="@string/title_activity_nameplayers" >
        </activity>
    </application>

</manifest>
